#include<bits/stdc++.h>
using namespace std;

int main(){
    // vector<int> v;
    // vector<int> v(10);
    // vector<int> v(10, -1);
    // vector<int> v2(v);
    // int a[5] = {1, 2, 3, 4, 5};
    // vector<int> v(a, a+5);
    vector<int> v = {1, 2, 3, 4, 5};
    for(int i=0; i<v.size(); i++)
        cout << v[i] << " ";
} 

//C++ er STL(Standard Template Library) er 1ta part 'vector(dynamic array)'